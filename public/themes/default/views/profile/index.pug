extends ../partials/layout

block content
  ol.breadcrumb
    li: a(href=website.domain) Home
    li.active Profile
    li.active= page

  if user
    include profile
  else
    .alert.alert-info This user has not joined our website yet
    